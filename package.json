{
  "name": "example-hacker-news",
  "description": "The default Marko starter app with TypeScript",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "marko": "^5.37.31"
  },
  "devDependencies": {
    "@marko/run": "^0.6.6",
    "@marko/run-adapter-netlify": "^2.0.1",
    "netlify-cli": "^20.0.2",
    "terser": "^5.39.0"
  },
  "private": true,
  "imports": {
    "#src/*": [
      "./src/*/index.ts",
      "./src/*.ts"
    ]
  },
  "scripts": {
    "dev": "marko-run",
    "build": "marko-run build",
    "preview": "marko-run preview",
    "deploy": "netlify deploy --build"
  }
}
