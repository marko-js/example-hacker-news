{
  "name": "example-hacker-news",
  "description": "The default Marko starter app with TypeScript",
  "version": "1.0.0",
  "type": "module",
  "dependencies": {
    "marko": "^6.0.62"
  },
  "devDependencies": {
    "@marko/run": "^0.8.0",
    "@marko/run-adapter-netlify": "^3.0.1",
    "netlify-cli": "^23.5.0"
  },
  "private": true,
  "imports": {
    "#src/*": [
      "./src/*/index.ts",
      "./src/*.ts"
    ]
  },
  "scripts": {
    "dev": "marko-run",
    "build": "marko-run build",
    "preview": "marko-run preview",
    "deploy": "netlify deploy --build"
  }
}
